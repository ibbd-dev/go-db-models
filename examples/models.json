{
    "queryRow": "off",
    "query": "off",
    "tables": [
        {
            "name": "ad_plan",
            "fields": ["id", "name", "status", "daily_budget", "start_date", "created_at"]
        },
        {
            "name": "ad_project",
            "fields": ["id", "plan_id", "name", "status", "remark"],
            "queryBy": "id"
        }
    ]
}
